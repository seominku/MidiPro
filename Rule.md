1. 구조적 규칙

계층 분리를 지켜라 — 위에서 그린 아키텍처처럼 MIDI I/O, 오디오 엔진, 시퀀서, GUI가 서로 직접 참조하지 않게 합니다. 예를 들어 신디사이저 엔진 코드 안에서 GUI 코드를 절대 include하지 않기.
인터페이스(추상 클래스)로 모듈 경계를 만들어라 — IMidiDevice, IAudioEngine 같은 인터페이스를 두면, 나중에 RtMidi를 다른 라이브러리로 바꿔도 상위 코드는 안 건드려도 됩니다.
하나의 클래스/함수는 하나의 책임만 (단일 책임 원칙) — 예: Track 클래스가 노트 저장, 파일 저장, 오디오 렌더링을 다 하게 하지 말고 각각 분리.

2. 네이밍/스타일 일관성

프로젝트 초반에 네이밍 컨벤션을 하나로 고정하세요 (예: 클래스는 PascalCase, 함수는 camelCase, 멤버 변수는 m_ 접두사). 중간에 바뀌면 나중에 다 뜯어고쳐야 함.
.clang-format 파일을 프로젝트 루트에 두고 자동 포맷팅 적용 — 스타일 논쟁 자체를 없앨 수 있어요.

3. 실시간 오디오 코드 특화 규칙 (중요!)
이건 일반 앱과 다른, MIDI/오디오 프로그램 특유의 규칙이에요:

오디오 콜백(realtime thread) 안에서는 절대 하지 말 것: 메모리 할당(new, malloc, std::vector::push_back 등), 뮤텍스 락, 파일 I/O, 로깅. 이걸 어기면 오디오 끊김(glitch/dropout)이 발생합니다.
오디오 스레드와 GUI/메인 스레드 간 데이터 교환은 락프리 큐(lock-free queue)나 아토믹 변수로 처리.
미리 버퍼를 다 할당해두고(pre-allocation), 실시간 처리 중에는 재사용만 하는 패턴을 씁니다.

4. 파일/모듈 조직

기능별 폴더 구조를 초반에 정하기:

src/
  midi/        (I/O, 파싱)
  audio/       (오실레이터, 필터, 엔진)
  sequencer/   (타이밍, 트랙)
  gui/         (UI 컴포넌트)
  core/        (공통 유틸, 자료구조)

헤더 파일에 로직을 다 넣지 말고 .h/.cpp 분리 유지 (템플릿 제외).

5. 문서화 습관

함수/클래스 위에 "왜"를 설명하는 주석을 남기세요. 코드는 "무엇을 하는지"는 보여주지만 "왜 이렇게 했는지"는 안 보여주니까요. (예: "여기서 뮤텍스 대신 아토믹을 쓴 이유: 오디오 스레드 블로킹 방지")
MIDI 메시지 포맷처럼 매직 넘버가 많은 도메인은 상수/enum으로 이름 붙이기 필수 (예: 144를 그냥 쓰지 말고 NOTE_ON = 0x90).

6. 테스트 가능하게 설계

MIDI 파싱, 타이밍 계산 같은 순수 로직은 오디오/GUI와 분리해서 유닛 테스트가 가능하게 만드세요. (Catch2, GoogleTest 등)
하드웨어 의존 부분(MIDI 장치, 오디오 출력)은 Mock으로 대체 가능하게 인터페이스화.

7. 버전 관리 습관

커밋은 작은 단위로, 메시지는 "무엇을 왜 바꿨는지" 명확히 (예: "Add note-on velocity scaling to fix quiet playback").
기능 단위로 브랜치 나누기 (feature/midi-learn, feature/piano-roll).

8. 점진적 확장에 대비

지금 "Phase 1"만 만들더라도, Phase 3의 신디사이저가 나중에 들어올 자리를 인터페이스로 비워두는 것이 핵심입니다. 처음부터 완벽할 필요는 없지만, 나중에 갈아엎지 않아도 되게 확장 지점(extension point)을 의식적으로 남겨두세요.